{
  "name": "driftq-clients-js",
  "version": "0.1.0",
  "description": "TypeScript SDK for DriftQ (produce, consume, admin).",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "require": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "test": "vitest run",
    "lint": "echo 'lint not configured yet'"
  },
  "engines": { "node": ">=18" },
  "files": ["dist"],
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/node": "^20.11.24",
    "typescript": "^5.6.3",
    "vitest": "^1.5.1",
    "rimraf": "^5.0.5"
  }
}
